---
title: About
---

- if data.try(:about)
  %section.about
    .about__photo
      - if data.about.try(:image).present?
        = image_tag data.about.image, class: 'about__image'
    .about__info
      - if data.about.try(:profile).present?
        - data.about.profile.each do |profile|
          .about__item
            %h1.about__title= profile.try(:title)
            %p.about__text= profile.try(:body)
